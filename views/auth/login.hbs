<body id="loginBody">

<div class="sectionBackground">
<div id="loginForm">
  <h2>Login</h2>
  

    <div class="row">
      <div class="col"></div>
      <div class="col">
         <form action="/auth/login" method="POST">
        <input
          type="text"
          class="form-control"
          name="Wizardname"
          placeholder="Your Wizard name"
          aria-label="Wizardname"
          bg-transparent
        />
      </div>
      <div class="col">
        <input
          type="password"
          class="form-control"
          name="password"
          placeholder="Your Password"
          aria-label="password"
        />
      </div>
      <div class="col"></div>
      
    </div>
  
  <div id="loginBtn"><button type="submit" class="btn customBtnDesgin">Log In</button></div>

    {{#if errorMessage}}
      <p class="error">{{errorMessage}}</p>
    {{/if}}
  </form>
</div>
<p id="intro">The Hidden Corridor is a famous secret room in Hogwarts, where students have been posting illicit materials for generations.  We can't tell you exactly where it is, but a good place to start looking would be behind the suit of armour on the 5th floor.. </p>
</div>
</body>